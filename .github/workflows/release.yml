name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          scope: '@ermis-network'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Extract version from tag
        id: version
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          VERSION=${TAG_NAME#v}
          echo "tag=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Releasing version: $VERSION"

      - name: Update SDK version
        run: |
          cd packages/sdk
          npm version ${{ steps.version.outputs.version }} --no-git-tag-version --allow-same-version

      - name: Update VCR Client version
        run: |
          cd packages/vcr-client
          npm version ${{ steps.version.outputs.version }} --no-git-tag-version --allow-same-version

      - name: Update React package version
        run: |
          cd packages/react
          npm version ${{ steps.version.outputs.version }} --no-git-tag-version --allow-same-version

      - name: Build all packages
        run: pnpm build --filter='./packages/*'

      - name: Run tests
        run: pnpm test --filter='./packages/*'
        continue-on-error: true

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Release ${{ steps.version.outputs.tag }}
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 📦 Packages Released

            ### @ermis-network/ermis-classroom-sdk v${{ steps.version.outputs.version }}
            TypeScript SDK for Ermis Classroom - Real-time video conferencing and collaboration

            **Installation:**
            ```bash
            npm install @ermis-network/ermis-classroom-sdk@${{ steps.version.outputs.version }}
            # or
            pnpm add @ermis-network/ermis-classroom-sdk@${{ steps.version.outputs.version }}
            # or
            yarn add @ermis-network/ermis-classroom-sdk@${{ steps.version.outputs.version }}
            ```

            ### @ermis-network/vcr-client v${{ steps.version.outputs.version }}
            TypeScript Client for Virtual Classroom API with full type support

            **Installation:**
            ```bash
            npm install @ermis-network/vcr-client@${{ steps.version.outputs.version }}
            # or
            pnpm add @ermis-network/vcr-client@${{ steps.version.outputs.version }}
            # or
            yarn add @ermis-network/vcr-client@${{ steps.version.outputs.version }}
            ```

            ### @ermis-network/ermis-classroom-react v${{ steps.version.outputs.version }}
            React hooks and components for Ermis Classroom SDK

            **Installation:**
            ```bash
            npm install @ermis-network/ermis-classroom-react@${{ steps.version.outputs.version }}
            # or
            pnpm add @ermis-network/ermis-classroom-react@${{ steps.version.outputs.version }}
            # or
            yarn add @ermis-network/ermis-classroom-react@${{ steps.version.outputs.version }}
            ```

      - name: Setup .npmrc for npm
        run: |
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > .npmrc

      - name: Publish @ermis-network/ermis-classroom-sdk
        run: |
          cd packages/sdk
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @ermis-network/vcr-client
        run: |
          cd packages/vcr-client
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @ermis-network/ermis-classroom-react
        run: |
          cd packages/react
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: |
          echo "## 🎉 Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Published Packages" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Package | Version | Registry |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|---------|----------|" >> $GITHUB_STEP_SUMMARY
          echo "| @ermis-network/ermis-classroom-sdk | ${{ steps.version.outputs.version }} | npm |" >> $GITHUB_STEP_SUMMARY
          echo "| @ermis-network/vcr-client | ${{ steps.version.outputs.version }} | npm |" >> $GITHUB_STEP_SUMMARY
          echo "| @ermis-network/ermis-classroom-react | ${{ steps.version.outputs.version }} | npm |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Installation" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "pnpm add @ermis-network/ermis-classroom-sdk@${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "pnpm add @ermis-network/vcr-client@${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "pnpm add @ermis-network/ermis-classroom-react@${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
